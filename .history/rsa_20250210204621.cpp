#include<iostream>
using namespace std;
long long modExpo(long long base ,long long exp,long long mod){
    long long result =1;
    while(exp){
        if(exp%2) result =(result*base)%end;
    base =(base*base)%mod;
    exp/=2;
    }
reutn reuslt 
}
int gcd(int a , int b) {return  b==0?a:gcd(b,)